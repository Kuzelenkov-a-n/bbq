<h2 class="mt-4 text-center">Изменение пароля</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="form mt-3">
    <%= f.label :email, class: "font-weight-bold" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field mt-3">
    <%= f.label :password, class: "font-weight-bold"  %> (Оставьте поля пустыми если не хотите их менять)
    <%= f.password_field :password, autocomplete: "Новый пароль", class: "form-control" %>
    <% if @minimum_password_length %>
      <em>Не менее <%= @minimum_password_length %> символов</em>
    <% end %>
  </div>

  <div class="field mt-3">
    <%= f.label :password_confirmation, class: "font-weight-bold"  %>
    <%= f.password_field :password_confirmation, autocomplete: "Новый пароль", class: "form-control" %>
  </div>

  <div class="field mt-3">
    <%= f.label :current_password, class: "font-weight-bold" %>
    <%= f.password_field :current_password, autocomplete: "Текущий пароль", class: "form-control" %>
  </div>

  <div class="actions mt-3 mb-3">
    <%= f.submit "Сохранить", class: "btn btn-primary my-1" %>
  </div>
<% end %>

<h3>Удаление аккаунта</h3>
<p>Что-то пошло не так? <%= button_to "Удалить мой аккаунт", registration_path(resource_name), data: { confirm: "Вы уверены?" }, method: :delete, class: "btn btn-warning my-1" %></p>

<%= link_to "Вернуться назад", :back %>
